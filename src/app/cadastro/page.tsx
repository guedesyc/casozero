'use client';
import Link from 'next/link';
import { ArrowRight, LockKeyhole, Mail, UserRound } from 'lucide-react';
import { FormEvent, useState } from 'react';

export default function SignupPage(){const [loading,setLoading]=useState(false);const [notice,setNotice]=useState('');const submit=(event:FormEvent)=>{event.preventDefault();setLoading(true);setNotice('');setTimeout(()=>{setLoading(false);setNotice('Cadastro demonstrativo concluído. A integração com autenticação e pagamento será conectada ao Supabase.');},700)};return <main className="auth-page"><section className="auth-card auth-card-wide"><Link href="/" className="auth-brand">CasoZero</Link><p className="eyebrow">Novo cadastro</p><h1>Comece sua rotina.</h1><p className="auth-lead">Crie seu acesso e configure o CasoZero para sua área profissional.</p><form onSubmit={submit}><label className="field"><span>Nome completo</span><div className="auth-input"><UserRound size={16}/><input required placeholder="Seu nome"/></div></label><label className="field"><span>E-mail profissional</span><div className="auth-input"><Mail size={16}/><input type="email" required placeholder="voce@empresa.com"/></div></label><label className="field"><span>Área de atuação</span><select defaultValue="Direito"><option>Direito</option><option>Fisioterapia</option><option>Psicologia</option><option>Arquitetura</option><option>Fonoaudiologia</option><option>Consultoria</option></select></label><label className="field"><span>Senha</span><div className="auth-input"><LockKeyhole size={16}/><input type="password" minLength={8} required placeholder="Mínimo de 8 caracteres"/></div></label><label className="field"><span>Confirmar senha</span><div className="auth-input"><LockKeyhole size={16}/><input type="password" minLength={8} required placeholder="Repita sua senha"/></div></label><button className="primary full" disabled={loading}>{loading?'Criando cadastro...':'Inscrever-se'} {!loading&&<ArrowRight size={16}/>}</button></form>{notice&&<p className="auth-notice">{notice}</p>}<p className="auth-switch">Já possui uma conta? <Link href="/login">Entrar</Link></p></section></main>}
