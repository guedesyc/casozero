'use client';
import Link from 'next/link';
import { ArrowRight, LockKeyhole, Mail } from 'lucide-react';
import { FormEvent, useState } from 'react';

export default function LoginPage(){const [loading,setLoading]=useState(false);const [notice,setNotice]=useState('');const submit=(event:FormEvent)=>{event.preventDefault();setLoading(true);setNotice('');setTimeout(()=>{setLoading(false);setNotice('A autenticação real será conectada ao Supabase na próxima etapa.');},700)};return <main className="auth-page"><section className="auth-card"><Link href="/" className="auth-brand">CasoZero</Link><p className="eyebrow">Acesso ao sistema</p><h1>Entre na sua conta.</h1><p className="auth-lead">Acompanhe seus atendimentos, casos e próximos passos.</p><form onSubmit={submit}><label className="field"><span>E-mail</span><div className="auth-input"><Mail size={16}/><input type="email" required placeholder="voce@empresa.com"/></div></label><label className="field"><span>Senha</span><div className="auth-input"><LockKeyhole size={16}/><input type="password" required placeholder="Sua senha"/></div></label><button className="primary full" disabled={loading}>{loading?'Entrando...':'Entrar'} {!loading&&<ArrowRight size={16}/>}</button></form>{notice&&<p className="auth-notice">{notice}</p>}<p className="auth-switch">Ainda não tem uma conta? <Link href="/cadastro">Inscrever-se</Link></p><Link href="/" className="auth-back">Voltar para apresentação</Link></section></main>}
